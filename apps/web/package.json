{
  "name": "homepage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-token-parity.mjs && vite build",
    "preview": "vite preview",
    "check:tokens": "node scripts/check-token-parity.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
